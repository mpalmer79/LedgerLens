# LedgerLens — Implementation gap analysis

**Status as of 2026-05-22.** Most recently updated alongside session 13 (correction memory). Specific to the files on `main` after PRs #23 (backend foundation), #24 (frontend workflow), and #25 (correction memory).

This document maps what is implemented, what is implied but missing, what needs to exist to call LedgerLens a working bookkeeping app, and how the remaining work should be sequenced.

## Status changes since the original draft

- **Backend foundation — shipped (PR #23).** Items #1–#8 and #10 in the priority table below are complete: lazy settings + `/health` + `/ready`, SQLAlchemy persistence layer, transaction intake (single, batch, CSV import), categorize endpoint with confidence routing, review queue (approve / correct / uncategorizable), audit trail on every state change, ledger CSV export.
- **Frontend workflow pages — shipped (PR #24).** Item #9 in the table: `/app`, `/transactions`, `/transactions/import`, `/transactions/[id]`, `/review`, `/ledger` are real, talk to the typed API client, and surface the live backend.
- **Correction memory — shipped (PR #25).** Item #12 in the table: deterministic, exact-key correction memory. Human corrections create `CorrectionMemory` rows; future transactions matching the same key categorize from memory at zero model cost; conflicts route to review; generic merchants are explicitly excluded. This is rule lookup over a model categorizer — not training, not fine-tuning, not embeddings.
- **Hybrid rules + model categorizer — shipped (PR #26).** Item #13 in the table: a deterministic rule layer sits between correction memory and the model. ~25 curated rules in `backend/src/ledgerlens/data/category_rules.json` validated against the active chart of accounts at load time. Strong matches (rule confidence ≥ auto threshold) auto-approve with `provider = rule_categorizer`, zero cost, and an explanation citing the matched rule id. Below-auto matches route to review. Conflicting matches route to review. New endpoints `GET /rules` and `GET /transactions/{id}/rule-matches`; new frontend page `/rules`. The pipeline order is now: **memory → rules → model → confidence routing → human review → audit.**
- **Portfolio-safe demo mode — shipped (PR #28).** New `CATEGORIZER_MODE` env var with values `demo_stub` (default) and `anthropic`. In demo mode the fallback layer is a deterministic zero-cost stub (`DemoStubCategorizer`) that returns `UNCATEGORIZABLE` so unmatched transactions land in human review. The `anthropic` SDK is never imported in demo mode (regression-tested). `/ready` now reports `categorizer.mode` and `anthropic.required_for_current_mode` honestly. New `docs/PORTFOLIO_DEPLOYMENT.md`. Closes the "deploy demos shouldn't cost real money" concern.
- **Product story overhaul — shipped (PR #29).** Landing page rewritten around the small-business bookkeeping problem with three business value cards and a recruiter-facing technical-credibility section. New `/demo` guided journey (seven narrative steps) driven by real backend calls — load samples, layered categorization, route-to-review, correct, watch memory replay, export ledger. New backend endpoints `GET /demo/{status,sample-transactions}` (safe in any mode) and `POST /demo/{seed,reset}` (guarded to `CATEGORIZER_MODE=demo_stub`). Page-level copy upgrades on `/app`, `/review`, `/corrections`, `/rules`, `/ledger`. New `docs/LINKEDIN_PROJECT_STORY.md`, `docs/PRODUCT_OWNER_REVIEW.md`, `docs/PRODUCT_STORY_OVERHAUL_PLAN.md`. README intro rewritten.
- **Trust metric — shipped (PR #30).** New product-level metric: "100% of finalized guided-demo ledger rows are verified before export." A finalized row counts as verified only when it came from a rule auto-approval, a correction-memory replay, or an explicit human review. Demo-stub results and unreviewed model auto-approvals are explicitly NOT verified. New `LedgerTrust` schema, `trust` block on `/ledger`, `model_provider` + `verified` columns on the CSV export, new `TrustPanel` component on `/demo` step 6 and `/ledger`. Landing page reframed: raw model accuracy demoted from the headline; trust panel becomes the product's headline number. Eval page gets an "Raw model accuracy is not the product's trust boundary" callout. New `docs/TRUST_METRIC.md`.
- **Conversion + story sprint — shipped (PR #31).** Homepage rebuilt around the verified-ledger trust card with a reusable visual pipeline (`TrustPipeline`), a 30-second Loom video section (with premium placeholder + script doc), and an About-Michael strip. New routes `/about` and `/technical-story`. `/demo` step 6 gets a `<DemoOutcome />` verdict card; `/ledger` gets an export warning + confirmation when unverified rows exist; `/app` empty state becomes a three-card "Start guided demo / Import / Technical story" panel; `/evals` gets a side-by-side Model-metric vs Product-metric trust-boundary callout. Site identity centralised in `lib/site.ts` with a test that pins down: LinkedIn + GitHub present, no email / phone / resume link in shared constants, no raw-accuracy headline. SVG favicon + OpenGraph metadata. New `docs/LOOM_WALKTHROUGH_SCRIPT.md`, `docs/CONVERSION_AND_STORY_OVERHAUL_PLAN.md`, `docs/FINAL_CONVERSION_REVIEW.md`. `docs/LINKEDIN_PROJECT_STORY.md` rewritten around the verified-ledger framing.
- **Launch readiness — shipped (PR #32).** Real 1200×630 OG PNG at `frontend/public/og-ledgerlens.png` (regeneratable SVG source alongside); `metadata.openGraph.images` + `metadata.twitter.images` wired in `app/layout.tsx`. Header "About Michael" promoted to primary text weight; hero CTAs reorganised (demo → technical story → About → GitHub) with a recruiter-relevance line below them. Trust card microcopy strengthened (56-px display number, uppercase label, deliberate "See raw model evals →" CTA). `/technical-story` gains a Reviewer-takeaway card and a Typical LLM wrapper vs LedgerLens comparison table. `/demo` gains a "What to look for" framing panel. `/ledger` gains a positive "Every finalized row is verified" confirmation when `verification_rate == 100%` (existing unverified warning preserved). README intro gets "Try it / read about it" pointer block. New `docs/LAUNCH_READINESS_PLAN.md`, `docs/FINAL_LAUNCH_READINESS_REVIEW.md` (Railway + LinkedIn checklists). `docs/LINKEDIN_PROJECT_STORY.md` expanded into a full launch package (main post, first comment, alternate hooks, recruiter DM, screenshot guidance, trust-metric explainer). New `lib/launch.test.ts` pins the OG-asset and site-copy contracts.
- **Generated walkthrough — shipped (PR #33).** New `GeneratedWalkthrough` component (pure CSS keyframes, no new deps) plays a six-scene 30-second loop of the workflow story directly in the browser. `VideoDemo` renders it as the fallback when `NEXT_PUBLIC_LOOM_URL` is empty (the previous static "coming soon" tile is gone) and still renders the Loom iframe when the env var is set. "Generated walkthrough" badge marks the surface so it's never mistaken for a real screen recording. New `/walkthrough` route renders the animation full-screen with no nav (noindex) so it can be captured with any screen recorder. `VideoDemo` also adds clear `/demo` + `/technical-story` CTAs under the video. New `docs/GENERATED_WALKTHROUGH.md` + `docs/GENERATED_WALKTHROUGH_PLAN.md`; `docs/LOOM_WALKTHROUGH_SCRIPT.md` gains an Option B section. Vitest config extended to include `.test.tsx` with the automatic JSX runtime so component tests work without React imports. 11 new frontend tests pin the contract: all six scene titles render, "100% verified finalized demo ledger" appears, "100% AI accuracy" does not, the Loom override still works, the CTAs render.
- **Responsive design pass — shipped (PR #34).** New shared `MarketingNav` component for `/`, `/about`, and `/technical-story` — desktop inline row at `≥ md`, compact "Demo →" CTA + accessible hamburger sheet below `md`. Hero h1 and trust-card "100%" now use `clamp()` so they fluidly scale between phones and desktops. Public page padding standardized to `px-4 sm:px-6 lg:px-8`. `/technical-story` LLM-wrapper comparison renders as stacked cards on mobile and the original table at `≥ sm`. AppShell workflow nav rewritten as a horizontal-scroll row on mobile (was awkwardly wrapping). `overflow-x-hidden` on public-page roots prevents gradient blobs from triggering accidental horizontal scroll. New `docs/RESPONSIVE_PASS.md` with the before/after table and breakpoint verification. 4 new `MarketingNav.test.tsx` tests pin the desktop-vs-mobile cluster contract. 51 frontend tests total pass.
- **Responsive QA — shipped (PR #35).** New `lib/page-content.test.ts` with 40 page-content contract assertions across `/`, `/about`, `/technical-story`, `/demo`, `/ledger`, `/evals`, and `/app`. Pins the verified-ledger framing, the reviewer-takeaway / LLM-wrapper comparison, the "what to look for" panel, the trust-warning + positive-verified copy on `/ledger`, the trust-boundary callout + model-vs-product framing on `/evals`, the three-card empty state on `/app`, and the absence of email / phone / resume / `mailto:` / `tel:` in `/about`. `docs/FINAL_LAUNCH_READINESS_REVIEW.md` gains a 10-minute manual responsive QA checklist (sections 10 + automated-checks-backing-the-manual-list table) covering mobile homepage / about / technical-story / demo / ledger / evals plus tablet homepage / app dashboard plus global horizontal-overflow / nav / tap-target / trust-copy checks. 91 frontend tests total.
- **Monthly cleanup assistant — shipped (PR #36).** Product identity reframed from "AI transaction categorizer" to "bookkeeping cleanup assistant." Three new frontend routes: `/cleanup` (six-step monthly cleanup checklist driven by real backend status), `/questions` (plain-English projection of the review queue with multiple-choice answers that record as `reviewer_note` via the existing `/review-queue/{tx}/correct|approve|uncategorizable` endpoints), `/handoff` (accountant handoff page with TrustPanel + CleanupImpactSummary + ready/needs-review/owner-answers/corrections sections + markdown export download). Backend `GET /handoff` + `GET /handoff/export.md` derived entirely from existing Transaction / CategorizationResult / ReviewDecision / CorrectionMemory / LedgerTrust data — no new persistence. New `CleanupImpactSummary` component renders a conservative time-saved estimate (1.5 min per deterministic auto-approval, 2.0 min per memory replay) with explicit "estimate" framing. AppShell nav adds Cleanup / Questions / Handoff. 7 new backend tests pin: empty-DB shape, ready vs needs-review split, owner-note pass-through, conservative impact math, markdown export sections + honesty disclaimers, audit-event write. 15 new frontend page-content contract tests pin: six guided steps, six workflow link targets, plain-English question templates (4 categories + default), all four required handoff sections, both download CTAs, the honesty footer ("workflow-level, not raw model accuracy" + "financial guarantee" disclaimer). New `docs/SMALL_BUSINESS_VALUE_AUDIT.md` explains the product-identity shift, the target user, the real monthly cleanup workflow, and the MVP scope. 141 backend + 106 frontend tests pass.
- **CSV import mapping wizard — shipped (this PR).** Closes the highest-priority gap from `SMALL_BUSINESS_UX_ROADMAP.md` §3 (#1). The old `/transactions/import` was a developer-style form that demanded the LedgerLens column schema; the new wizard is a **five-step mobile-first flow** (Upload → Preview → Map columns → Validate → Imported) that turns a real-bank-shaped CSV into the existing endpoint's normalized format client-side. **Drag-and-drop dropzone + file-picker fallback + sample CSV download + "Load sample into wizard" shortcut**, all backed by Papa Parse (new dep, ~30 KB gzipped). New `frontend/public/samples/granite-state-bank-sample.csv` ships **15 fictional rows** in real-bank shape (`Posted Date / Description / Debit / Credit / Account / Reference`) from the Granite State Auto Repair scenario, exercising the debit/credit mapping mode. Auto-detection covers Date / Description / Amount / Debit / Credit / Merchant / Memo / Reference / Account columns by case-insensitive token-stripped matching ("Posted Date", "posted_date", "POSTED-DATE", "PostedDate" all hit the same candidate); every dropdown is user-overridable. **Two amount modes**: signed (one column) or debit/credit (two columns, auto-suggested when both headers present + no explicit Amount column). Honors the backend's sign convention — debit values become negative on submit, credit values become positive. Handles parentheses as negative `(150.00) → -150.00`, strips currency symbols + thousands commas, supports `YYYY-MM-DD` / `MM/DD/YYYY` / `MM/DD/YY` (expands to 20YY) / `DD/MM/YYYY` (when day > 12). **Plain-English per-row error messages** with valid / invalid / blank-rows-skipped counters + invalid-row highlighting; only valid rows submitted; invalid rows stay client-side so the user can fix the mapping without re-uploading. **Zero backend changes** — wizard builds a normalized CSV blob in the exact column shape the existing `POST /transactions/import` endpoint expects and submits via the existing `importCsv()` client function. **Demo safety**: original top-of-page warning preserved + a new second amber reminder on the validate step just above the import button; 1 MB client-side cap (backend's 5 MB cap is the backstop); non-CSV files rejected with friendly error. **Post-import**: success panel with "Start monthly cleanup → /cleanup" primary CTA + "Review transactions" secondary + "Import another file" tertiary. **219 frontend tests passing** (was 179, +40 — 35 new CSV-import logic tests in `src/lib/csv-import/csv-import.test.ts` covering parsing / header detection / date + amount normalization / signed + debit/credit modes / source-row preservation / blank-row skip / build-normalized-CSV header contract; +5 new `/transactions/import` page-content assertions; +1 new sample-CSV existence assertion). Backend tests unchanged at 202. New `docs/CSV_IMPORT_WIZARD_AUDIT.md`, `docs/CSV_IMPORT_MAPPING_WIZARD.md` (reference), `docs/CSV_IMPORT_MAPPING_WIZARD_REVIEW.md` (sprint review); `SMALL_BUSINESS_UX_ROADMAP.md` marks the CSV-wizard item shipped. Honesty contracts preserved: demo upload warning intact + reinforced, no production-accounting-software claim, no "100% AI accuracy" anywhere, demo-stub regression test still passes.
- **Productization boundary + accounting-language audit + production-readiness docs — shipped (PR #45).** A major honesty-and-framing PR (not a feature PR) responding to a senior review that flagged accounting-overclaim risk and production-readiness hand-waving. Buyer-facing copy across homepage, `/handoff`, `/ledger`, `/demo`, `/technical-story`, `TrustPanel`, `GeneratedWalkthrough`, README, LinkedIn doc, and `docs/TRUST_METRIC.md` rewritten: **"verified ledger" → "procedurally verified rows"**, **"ledger export" → "reviewed categorization export"**, **"Postgres-ready" → "Postgres-compatible models"** with explicit "production migration management not implemented" qualifier. Every trust-metric surface now includes the procedural-verification clarification: "Verification is procedural… workflow trust boundary, not a guarantee of accounting or tax correctness, and not a substitute for CPA review." `/technical-story` gains a new "Production-readiness boundary" section that names the gaps (no auth, no Alembic, no PII redaction, no double-entry) + cross-links to three new boundary docs. `/transactions/import` gains a prominent amber warning panel: "Public demo — do not upload real bank data… no authentication and no tenant isolation on this deploy." `/evals` trust-boundary section gains a reminder that eval numbers measure categorization behavior, not accounting correctness. `README.md` gets a new top-of-file pair "What LedgerLens is" / "What LedgerLens is NOT" + a "Public demo warning" + a "Security and production-readiness status" section + a "Production roadmap" section + a "Test / CI status" section. **Six new docs**: `PRODUCTIZATION_GAP_AUDIT.md` (17-gap audit with severity ratings, distinguishing portfolio demo from production SaaS), `ACCOUNTING_LANGUAGE_AUDIT.md` (risky-terms-found table + safer-replacement glossary), `ACCOUNTING_DOMAIN_BOUNDARY.md` (what LedgerLens does vs does not in accounting-domain terms; what production accounting correctness would require), `SECURITY_AND_PRODUCTION_READINESS.md` (5-phase production roadmap: A=auth/tenant, B=observability, C=migrations/backups/retention, D=PII redaction, E=dependency scanning), `SMALL_BUSINESS_UX_ROADMAP.md` (concrete designs for CSV mapping wizard, account-mapping wizard, mobile-first review queue), `PRODUCTIZATION_BOUNDARY_REVIEW.md` (sprint review). **Lightweight security fixes**: `backend/src/ledgerlens/main.py` startup `except Exception: pass` replaced with `logger.exception(...)` (resilience preserved, error no longer silently swallowed); new `.github/dependabot.yml` shipping weekly npm + pip updates and monthly GitHub Actions updates with grouped updates for the major frameworks. **202 backend tests passing** (unchanged) + **179 frontend tests passing** (was 161, +18 — explicit "not accounting software" + procedural-verification framing on homepage, "not CPA-reviewed" + procedural disclaimer on /handoff, production-readiness boundary section on /technical-story, "do not upload real bank data" warning on /transactions/import, "Procedurally verified rows" replaces old wording on /evals + GeneratedWalkthrough + VideoDemo + demo step 6, plus a new `productization-docs.test.ts` file with 7 tests asserting the 6 boundary docs ship + Dependabot config exists + covers npm/pip/github-actions). Honesty contracts preserved + strengthened: raw Haiku accuracy still headline on /evals, sample-scenario disclaimers untouched, "Not tax advice or substitute for accounting review" disclaimer expanded to "not CPA-reviewed books… not a substitute for accounting review" on /handoff, demo-stub regression test still passes, Anthropic SDK still not imported in demo mode.
- **Parts-vendor rule Batch #1 — shipped (PR #44).** First end-to-end demonstration of the eval-driven rule-improvement loop: identified gap → shipped safe rules → re-ran eval → measured honestly → updated roadmap. **8 new rules** in `category_rules.json` for NAPA, AutoZone, O'Reilly, Advance Auto, LKQ, Carquest (intent: `parts_inventory`) plus tire distributors (TIRERACK, SNOW TIRE BULK, GRANITE STATE TIRE — new intent: `tires_inventory`). All five business rule maps updated: `DEFAULT_INTENT_MAP` + `GRANITE_STATE_INTENT_MAP` gain `parts_inventory` + `tires_inventory` overrides; `AUTO_REPAIR_EVAL_INTENT_MAP` adds `tires_inventory → 1070`; `COFFEE_SHOP_EVAL_INTENT_MAP` + `DESIGN_AGENCY_EVAL_INTENT_MAP` add both intents to `block_fallback_intents` so a NAPA charge on a coffee shop never silently classifies as Green Coffee. **Fresh committed eval artifacts** (`2026-05-24-rule-categorizer-{v1,mapped-v1}.json` + `comparison.{json,md}`) show the measured impact: auto-repair coverage **5% → 19%**, auto-repair mapped-accuracy **40% → 84%** (16 of 19 mapped rows correct), auto-approve accuracy **22.2% → 44.7%**, overall rules-only-mapped accuracy **2.32% → 6.95%**. Coffee-shop / design-agency unchanged (by design; the new patterns don't appear in those datasets, and block_fallback prevents silent miscategorization). Safety: fuel stations still resolve to `fuel_vehicle` (NOT parts), bare `AUTO` text does not match, Amazon / Home Depot / Lowe's remain owner-question-routed at low confidence. `/rules` page gains a Batch #1 section naming the new rules, intents, and safety boundary. `/evals` page gains a Batch #1 note under the Business-specific rule mapping heading with the honest before/after numbers (22.2% → 44.7%). `RULE_GAP_ANALYSIS.md` updated with a batch-status section marking Batch #1 shipped + revised next-batch ordering (Batch #3 software intent split promoted to #1 for design-agency). **202 backend tests passing** (was 183, +19 — 12 new parts-rule tests in `tests/test_parts_vendor_rule_batch.py` covering each vendor pattern, fuel-station safety, generic-auto safety, block-fallback on coffee-shop + design-agency, Granite State map coverage, Amazon unchanged + 4 production-path tests in `tests/api/test_parts_vendor_production_path.py` covering `find_rule_match` for NAPA / AutoZone / LKQ / Granite State Tire + 3 from the test_rule_mapping.py update). **161 frontend tests passing** (was 159, +2 — /rules Batch #1 description + intents + safety language, /evals Batch #1 measured-impact note). One existing test updated (`test_granite_state_specific_overrides` — the default map now carries parts_inventory). New `docs/PARTS_VENDOR_RULE_BATCH_AUDIT.md`, `docs/PARTS_VENDOR_RULE_BATCH_REVIEW.md`, `docs/SAFE_RULE_IMPROVEMENT_LOOP.md`, `docs/PARTS_VENDOR_RULE_BATCH_FINAL_REVIEW.md`. Honesty preserved: raw Haiku accuracy still 62.9% / 41.9%, design-agency 0% mapped-accuracy still reported plainly, block_fallback reported as safe abstention, demo-stub regression still passes.
- **Multi-business mapped-rule eval coverage — shipped (PR #43).** Completes the multi-business story for the mapped rule eval mode. Adds **`COFFEE_SHOP_EVAL_INTENT_MAP`** and **`DESIGN_AGENCY_EVAL_INTENT_MAP`** to `business_rule_maps.py`, each calibrated to its eval dataset's actual COA. `EVAL_BUSINESS_MAP_IDS` now registers all three eval business ids (`auto-repair`, `coffee-shop`, `design-agency`). New **`block_fallback_intents: frozenset[str]`** field on `BusinessRuleMap` lets a business explicitly refuse the rule's seed-COA default when it would silently miscategorize on the dataset COA — eval categorizer's `_resolve_code` honors it. `mapping_metrics(tx_to_business=...)` returns a **`per_business`** breakdown plus a **`summary`** block naming the best / weakest business and the top unmapped intents overall. Harness threads `tx_to_business` to the overall slice. Fresh eval artifacts committed: `2026-05-24-rule-categorizer-{v1,mapped-v1}.json` and `2026-05-24-comparison.{json,md}` with the actual deltas — overall rules-only-mapped accuracy 0.7% → **2.3%**, auto-approve accuracy 0.0% → **22.2%**, adversarial accuracy 0.0% → **6.5%**. Per-business: auto-repair 5 mapped / 2 correct (40%), coffee-shop 28 mapped / 5 correct (17.9%), design-agency 39 mapped / **0 correct** (the dataset uses 5 software subaccounts; rules need finer intents — documented in `RULE_GAP_ANALYSIS.md`). New `docs/RULE_GAP_ANALYSIS.md` reads from the mapped run and outputs a concrete engineering roadmap (parts-vendor rule batch, payroll-service rules, software intent split, etc.). `/evals` Business-specific rule mapping section gains a "Per-business mapped-rule breakdown" table with mapped/fallback/review/correct-when-mapped per business and a "best / biggest rule gap" summary line. `/rules` cross-link updates to "View multi-business rule evals" + mentions all three eval businesses. **183 backend tests passing** (was 172, +11 — coffee-shop + design-agency registration, override resolution, `block_fallback` behavior on both businesses, `is_fallback_blocked` method, `mapping_metrics` per-business aggregation). **159 frontend tests passing** (was 157, +2 — `/evals` per-business breakdown table, `/rules` multi-business CTA + business names). New `docs/MULTI_BUSINESS_RULE_MAPPING_AUDIT.md`, `docs/MULTI_BUSINESS_MAPPED_RULE_EVALS.md`, `docs/RULE_GAP_ANALYSIS.md`, `docs/MULTI_BUSINESS_RULE_MAPPING_REVIEW.md`. Honesty contracts preserved: raw Haiku accuracy still 62.9% / 41.9% adversarial, design-agency 0% mapped-accuracy reported plainly (no cherry-picking), `block_fallback` reported as safe abstention not misclassification, workflow-level trust language intact, demo-stub regression test still passes.
- **Mapped-rule eval harness — shipped (PR #42).** Wires the per-business rule intent mapping from PR #41 into the evaluation harness. Two new CLI modes: `rules-only-mapped` (mapped variant of `rules-only`) and `hybrid-rules-model-mapped`. Existing generic baselines (`rules-only`, `hybrid-rules-model`) preserved. `RuleOnlyCategorizer` accepts `use_business_mapping=True` + an optional `business_id` and stamps `matched_rule_intent` + `mapping_outcome` (`"mapped" / "fallback_to_default" / "routed_to_review"`) on each prediction. `CategorizationResult` (eval-side) gains those two optional fields. New `AUTO_REPAIR_EVAL_INTENT_MAP` in `business_rule_maps.py` keyed to the auto-repair eval dataset's COA (parts_inventory → 1050, software_subscription → 6190, internet_telecom → 6250) — distinct from the existing `GRANITE_STATE_INTENT_MAP` which targets the seed COA. New `metrics.mapping_metrics()` aggregates per-prediction provenance into `mapped_intent_count / fallback_to_default_count / routed_to_review_count / unmapped_intent_count / mapping_override_count / correct_when_mapped / correct_when_fallback / top_unmapped_intents / top_rule_intents`. `RunMetrics.MetricsSlice` gains a `mapping` block. `compare.py` gains a new "Mapping" column + extended JSON output. **Committed fresh eval artifacts** for the three free modes against the v0 dataset: `2026-05-24-rule-categorizer-v1.json`, `2026-05-24-rule-categorizer-mapped-v1.json`, `2026-05-24-comparison.{json,md}`. **Real measured deltas**: generic rules 0.0% / mapped rules 0.7% overall; auto-approve accuracy 0.0% → 7.4% (the only signal the rule layer has on this dataset); 5 rows resolved via mapping where the generic mode dropped them at COA-validation; top unmapped intents (`merchant_fees`, `software_subscription`, `office_supplies`) now visible. `/evals` page gains a new "Business-specific rule mapping" section (with anchor `#business-specific-rule-mapping`) showing the side-by-side comparison, top unmapped intents, and the explicit "mapped rules do not replace review · workflow-level not raw model accuracy" callout. `/rules` page gains a "View rule-mapping evals →" CTA linking to the evals anchor. **172 backend tests passing** (was 163, +9 — new `tests/evals/test_mapped_rule_eval.py` covering mode registration, mapped resolution, fallback path, route-to-review path, unmapped-business fallback, backward compat, and mapping_metrics aggregation). **157 frontend tests passing** (was 154, +3 — `/evals` business-specific-rule-mapping section, anchor, "do not replace review" + workflow-level disclaimer, `/rules` cross-link). New `docs/MAPPED_RULE_EVAL_AUDIT.md` (planning audit), `docs/MAPPED_RULE_EVALS.md` (eval-mode methodology reference), `docs/MAPPED_RULE_EVAL_REVIEW.md` (sprint review). Honesty contracts preserved: raw Haiku accuracy (62.9% / 41.9% adversarial) still the headline model number, `routed_to_review` reported as safe abstention (not misclassification), workflow-level trust language intact, fictional sample scenario + not-tax-advice disclaimers intact, demo-stub regression test still passes.
- **Owner Answers v2 + per-business rule mapping — shipped (PR #41).** Two coordinated improvements that together strengthen the handoff package and make deterministic categorization measurable per business. **Owner Answers v2** extends `ReviewDecision` with six new nullable columns (`owner_question_key / owner_question_text / owner_answer_label / owner_note / suggested_resolution / accountant_follow_up_required`); the three review-queue endpoints (`/approve`, `/correct`, `/uncategorizable`) accept these via a shared `OwnerAnswerFields` Pydantic mixin; `HandoffOwnerAnswer` gains date / amount / currency + all six v2 fields; the handoff markdown export splits the "Questions answered by owner" section into a labelled v2 block (question, owner answer, owner note, suggested resolution, follow-up flag) followed by a "Legacy review notes (pre-v2)" block so old `reviewer_note`-only rows still surface. `/questions` page templates gain stable `key`s (`unknown_ach_transfer / marketplace_purchase / home_improvement_store / owner_transfer / venmo_or_cash_movement / parts_vendor / customer_deposit / default_uncertain_transaction`); each `Answer` carries optional `accountantFollowUp` + `suggestedResolution`; the page renders a new owner-note `<textarea>` per card, helper copy explaining the answer goes to the handoff package, and an amber accountant-review treatment on follow-up answers. **Per-business rule mapping** adds an optional `intent: str | None` to the `Rule` dataclass + JSON loader; new `backend/src/ledgerlens/data/business_rule_maps.py` ships `BusinessRuleMap` + `DEFAULT_INTENT_MAP` + `GRANITE_STATE_INTENT_MAP` + `resolve_category_for_intent(intent, *, business_id, fallback_code)`. All 25 bundled rules tagged with intents (`software_subscription`, `office_supplies`, `fuel_vehicle`, `travel`, `meals_entertainment`, `merchant_fees`). Categorize service resolves a rule's effective code via the active business mapping after a match; missing-mapping fallback is the rule's own `category_code`, missing-COA-category fallback also falls back. `RuleOut` gains `intent + mapped_category_code + mapped_category_name`; `RuleListOut` gains a `mapping: BusinessRuleMapOut` snapshot exposing the active business's full intent→code grid. `/rules` page rebuilt with shared loading/error states, a new "Active business mapping" card naming Granite State Auto Repair + showing the full intent grid, and a per-row "overrides rule default" annotation when the mapping changes the rule's code. **163 backend tests passing** (was 146, +17 — 7 owner-answer persistence + handoff surfacing + markdown rendering + legacy v1 handling + accountant-follow-up flag, 8 rule-mapping unit tests covering loader + resolver + Granite State overrides + unknown-business fallback, 2 e2e via the rules API). **154 frontend tests passing** (was 146, +8 — Owner Answers v2 template keys, owner-note field, helper copy, accountant-review treatment, handoff structured rendering, /rules intent + mapped-category + mapping-card surfaces, ErrorState/LoadingState usage on /rules). New `docs/OWNER_ANSWERS_AND_RULE_MAPPING_AUDIT.md`, `docs/OWNER_QUESTIONS_V2.md`, `docs/PER_BUSINESS_RULE_MAPPING_DESIGN.md`, `docs/RULE_MAPPING_REVIEW.md`, `docs/OWNER_ANSWERS_AND_RULE_MAPPING_REVIEW.md`. Honesty contracts preserved: no "100% AI accuracy", fictional sample scenario badges + "not tax advice" disclaimer intact, trust metric still workflow-level (the new follow-up flag is metadata, not a verification gate), Anthropic SDK still never imported in demo mode.
- **Public-demo hardening — shipped (PR #40).** Tightened the reliability layer so the public demo doesn't feel brittle when the backend is slow, unavailable, empty, or returns an error. **`apiFetch`** gains a 10s default timeout (AbortController-driven), retry-once for safe GETs on transient failures (network error, timeout, 408, 425, 429, 5xx), and a strict no-retry policy on mutations. `ApiError` enriched with `retryable: boolean` + a plain-English `userMessage: string` (e.g. "LedgerLens could not reach the demo backend"). New `ApiFetchOptions` exposes `timeoutMs / retries / retryDelayMs / signal` per call. **Three reusable state components** in `frontend/src/components/ui/DataState.tsx`: `<LoadingState>` (accessible status role + label + spinner), `<EmptyState>` (icon + title + body + primary/secondary actions), `<ErrorState>` (variant + retry + secondary action + collapsed technical details that consume the `ApiError` envelope automatically). **Five exposed pages hardened**: `/cleanup`, `/questions`, `/handoff`, `/demo`, `/app` — `useCallback`-wrapped `load()` so `<ErrorState>` retry actually re-runs the fetch; shared loading / empty / error UI; `/questions` gets a per-card `saveErrors` map so one failed save no longer blanks the queue; `/handoff` downloads now button-driven `handleDownload` that `HEAD`-probes the URL and surfaces per-button inline errors instead of silently downloading nothing on a 500; `/demo` reset shows "Resetting…" and disables while in flight, seed handler short-circuits on double-click, scenario load decoupled so a `/demo/scenario` 503 doesn't break the page; `/app` page-level errors gain a retry handler. **CI enforcement**: `.github/workflows/ci.yml` frontend job now runs `npm test -- --run` between lint and build (previously: lint + build only). 146 frontend tests now passing in CI (was 119 locally, +27 — 9 DataState component contracts, 5 retry-behavior cases, 3 ApiError-envelope cases, 9 page-content contracts across the five hardened pages, plus the lock-in that no test claims "100% AI accuracy"). 146 backend tests unchanged (sprint is frontend-only); Anthropic-SDK-not-imported regression still passes. New `docs/FRONTEND_RELIABILITY_AUDIT.md` (planning), `docs/PUBLIC_DEMO_RELIABILITY.md` (reference), `docs/DEPLOYMENT_SMOKE_TEST.md` (17-step manual checklist + 5 failure-mode spot checks), `docs/PUBLIC_DEMO_HARDENING_REVIEW.md` (sprint review).
- **Sample-business scenario — shipped (PR #39).** Replaced the 13-row generic demo dataset with the fictional **Granite State Auto Repair** monthly cleanup scenario: 42 March 2026 transactions spanning parts (NAPA, AutoZone, O'Reilly, Advance Auto, LKQ, Granite State Tire), payroll (ADP bi-weekly + tax + health insurance), utilities and rent (Eversource, Comcast, Waste Management, NH Property Management, Manchester Water), software (QuickBooks, Mitchell1, Google Workspace), fuel (Shell, Irving, Mobil), insurance and finance (Hanover, TD Bank, First Citizens), revenue deposits (Stripe payout x2, Square, customer checks x2, cash), and ambiguous rows (ACH transfer, paper check, Amazon, Costco, Home Depot, Lowe's, Venmo, OWNER TRANSFER, ATM withdrawal). New `ledgerlens.data.sample_scenario` module centralises the fictional business identity (name / location / month / disclaimer / handoff filename). New `GET /demo/scenario` endpoint exposes the profile. `HandoffOut` gains an optional `scenario` field that's populated whenever the handoff contains any demo-sourced rows; the markdown export uses a scenario-aware heading + filename (`handoff-granite-state-auto-repair-2026-03.md`) and always carries the "not tax advice or a substitute for accounting review" disclaimer. `/questions` gets four new templates (parts vendors, owner transfers / venmo / ATM, home-improvement stores, customer deposits / revenue). `/demo` gains a scenario card; `/cleanup` and `/handoff` surface the business name + "Sample data" badge when demo data is present; the homepage example handoff preview rebrands to "Example: Granite State Auto Repair · March 2026 cleanup before accountant handoff" with the audit-doc illustrative numbers (42 / 28 / 10 / 4 / 5 / 100%). `GeneratedWalkthrough` swaps to auto-shop-flavored vendor names (NAPA, ADP Payroll, Shell, Amazon, ACH 41281) and the new filename. New `docs/SAMPLE_BUSINESS_SCENARIO_AUDIT.md`, `docs/SAMPLE_BUSINESS_SCENARIO.md`, `docs/examples/granite-state-auto-repair-handoff.md`, and `docs/SAMPLE_SCENARIO_REVIEW.md`. 146 backend tests pass (was 141, +5 — scenario endpoint, 42-row seed, demo-stub regression, no-scenario-without-demo-data handoff, scenario-aware handoff markdown). 119 frontend tests pass (was 113, +6 — homepage scenario presence, sample-cleanup-scenario card, /cleanup scenario badge, /handoff scenario header, /questions new templates, GeneratedWalkthrough auto-shop vendor names). No new paid API behavior; the regression test that asserts the Anthropic SDK is never imported after a demo-mode call still passes.
- **Walkthrough rescript — shipped (PR #38).** The 30-second `GeneratedWalkthrough` animation now tells the **cleanup → handoff** story end-to-end. Six new scenes: Monthly bookkeeping cleanup (messy bank activity) → Obvious vendors handled first (rules + memory tags) → Uncertain rows become owner questions (plain-English choices, no COA codes) → Answers create accountant context (owner-answer review notes) → Verified rows stay separated from unresolved items (Ready-for-accountant vs Needs-review split) → Export the accountant handoff package (`handoff-2026-03.md` preview + the same 100% verified trust card). Section header on the homepage shifts to "Watch the cleanup-to-handoff flow"; `VideoDemo` fallback CTAs flip to `/cleanup` (primary) and `/handoff` (secondary outline) with `/technical-story` retained as a smaller text link. `/walkthrough` helper relabeled. `docs/LOOM_WALKTHROUGH_SCRIPT.md` rewritten to record the cleanup → handoff narration (with the prior verified-ledger script archived as Option C). `docs/GENERATED_WALKTHROUGH.md` updated to the new scene table. New `docs/WALKTHROUGH_HANDOFF_RESCRIPT_AUDIT.md` + `docs/WALKTHROUGH_HANDOFF_STORY_REVIEW.md`. Honesty contracts preserved verbatim (no "100% AI accuracy," workflow-level trust phrasing intact, "Generated walkthrough" badge intact, Loom override path intact, `prefers-reduced-motion` falls back to the static handoff frame). 113 frontend tests pass (was 110, +3 net new walkthrough / VideoDemo assertions).
- **Handoff story sprint — shipped (PR #37).** Marketing layer caught up to the cleanup-assistant product identity. Homepage hero rewritten: eyebrow now says "Monthly bookkeeping cleanup assistant," headline names the **verified handoff** as the deliverable, primary CTA flips from `/demo` to `/cleanup`, secondary CTA promotes `/handoff` (heavy outline button). New Before / After section + "What you get in the handoff package" card + polished **Example handoff preview** with six labelled tiles and four section blocks (illustrative numbers tagged explicitly). Trust card explainer now reads "*before they appear in the handoff package*." MarketingNav reorders Cleanup → Handoff → Demo → Technical story → Evals → About → GitHub; mobile compact CTA flips to "Cleanup →". `/cleanup` intro rewritten as "Use this checklist once a month." `/handoff` export area becomes two cards with per-export "use this for X" explanations + a new "not tax advice or a substitute for accounting review" honesty line. `/app` empty state gains a fourth card with **Open cleanup assistant** as the primary brand-bordered CTA. README intro + "What problem does it solve?" + "What is the handoff package?" sections. LinkedIn doc gains a long version (rewritten around cleanup → handoff) and a short LinkedIn-skim version. New `docs/HANDOFF_PRODUCT_STORY_AUDIT.md` + `docs/HANDOFF_STORY_REVIEW.md`. 110 frontend tests pass (was 106, +4 new homepage / MarketingNav / app-empty-state contracts).

The priority table below is kept in its original form for traceability — items 1–10, 12, and 13 are now done; items 11 and 14 onward remain as the forward backlog. Item #15 (security baseline) gains the portfolio-mode toggle as a partial credit toward credible deploy operations. The next natural milestone is **per-tenant rule generation** so rules-only and hybrid eval modes can be benchmarked meaningfully against the synthetic dataset.

---

## 1. What is actually implemented

### Backend (`backend/`)

- **`ledgerlens.main`** — a FastAPI app with exactly one route: `GET /health`. Returns `{"status": "ok", "service": "ledgerlens-api"}`. Constructed in `create_app()`; module-level `app = create_app()` runs `get_settings()` at import.
- **`ledgerlens.config`** — Pydantic Settings reading `ANTHROPIC_API_KEY`, `DATABASE_URL`, model identifiers, threshold constants, log level, CORS origins, and app version. Both `anthropic_api_key` and `database_url` are required strings with no defaults. `get_settings()` is `lru_cache`-wrapped; calling it without env vars set raises `ValidationError`.
- **`ledgerlens.categorizers.base`** — `Categorizer` Protocol (`name: str` + `categorize(transaction, business, chart_of_accounts) -> CategorizationResult`) and the `CategorizationResult` Pydantic model.
- **`ledgerlens.categorizers.stub`** — `StubCategorizer` returning the first expense account at confidence 0.5.
- **`ledgerlens.categorizers.claude_haiku`** — `ClaudeHaikuCategorizer` calling Anthropic Haiku via tool_use, parsing the response into `CategorizationResult`, with one retry on validation failure and `UNCATEGORIZABLE` fallback on API error.
- **`ledgerlens.categorizers.prompts`** — `build_system_prompt()` and `build_user_prompt()`, plus `format_amount()`.
- **`ledgerlens.evals`** — schemas, loader, metrics, harness, writer, and CLI (`python -m ledgerlens.evals.run`). All read-side: no database.

### Eval pipeline

- Synthetic v0 dataset: 3 businesses, 302 transactions, 31 adversarial, committed under `evals/datasets/v0/`.
- Stub run committed at `evals/runs/2026-05-21-stub-v1.json` (9.3% overall).
- Claude Haiku run committed at `evals/runs/2026-05-22-claude-haiku-v1.json` (62.9% overall, 65.3% non-adversarial, 41.9% adversarial, $0.34/100 transactions, p95 latency 5,952 ms).
- GitHub Actions workflow `.github/workflows/eval.yml` triggers runs manually and auto-commits artifacts.

### Frontend (`frontend/`)

- Landing page `/` — hero with 3D transaction-card carousel (Three.js), "What am I looking at?" intro, three pillars, eval-results CTA (Featured), live API-health widget, footer.
- Eval dashboard `/evals` — reads `evals/runs/*.json` at build time; renders headline metrics, stub-vs-Haiku comparison chart (Recharts), reliability scatter, per-business table, adversarial deep-dive cards.

### Infrastructure

- Backend `Dockerfile` (single-stage Python 3.12) and Railway config.
- Frontend `Dockerfile` (single-stage Node 20) with `evals/` copied into the build context so the dashboard loads real artifacts.
- Root `railway.toml` for the frontend, `backend/railway.toml` for the backend.
- CI workflow `.github/workflows/ci.yml` runs ruff / ruff format / mypy strict / pytest on backend, lint + build on frontend.

### Docs

- 15 accepted ADRs covering decisions through 0015 (with 0008 skipped — no ADR exists for that number).
- `docs/ARCHITECTURE.md` — 13-section design spec, written aspirationally; some sections describe the planned system, not the shipped one.

---

## 2. What is implied or claimed but not implemented

### Backend application surface (the biggest gap)

The repo positions LedgerLens as "an AI-assisted transaction categorization system" with "categorization → confidence routing → human review → corrections → audit." None of that workflow exists in the backend application yet — only as eval-side batch processing reading committed JSON.

Specifically missing:

- **No persistence layer.** SQLAlchemy and Alembic are in `pyproject.toml`, but no models, sessions, migrations, or repositories exist. The settings loader expects a `DATABASE_URL` and there's no code that uses it.
- **No transaction endpoints.** No `POST /transactions`, no `GET /transactions`, no CSV import. The product can't accept a transaction from a user.
- **No categorization endpoint.** The `ClaudeHaikuCategorizer` exists, but there's no HTTP route that invokes it on a stored transaction.
- **No review queue.** `ARCHITECTURE.md §4` describes routing thresholds and `LEDGERLENS_AUTO_QUEUE_THRESHOLD = 0.90`, `LEDGERLENS_REVIEW_QUEUE_THRESHOLD = 0.60` are in settings, but no endpoint applies these thresholds to a stored result.
- **No corrections loop.** The "learns from corrections" story doesn't have a `corrections` table or a retrieval lookup.
- **No audit trail.** Settings has `LOG_LEVEL` but no AuditEvent model, no audit endpoint.
- **No ledger export.** The categorized output that bookkeepers would need is not exportable.

### Backend startup robustness

- `app = create_app()` calls `get_settings()` at import time. Without `ANTHROPIC_API_KEY` and `DATABASE_URL` set, importing `ledgerlens.main` raises `ValidationError`. Tests work around this with `backend/tests/conftest.py` setting dummy env vars. The Railway deployment requires both vars even though only categorization actually needs the API key.
- `/health` indirectly depends on `get_settings()` succeeding because the route is registered after settings load.
- There is no `/ready` endpoint distinguishing process-up from dependencies-ready.

### Frontend product surface

- Frontend has two real pages (`/` and `/evals`). The product workflow — transaction list, review queue, ledger view, import — is not implemented.
- The eval dashboard reads JSON at build time; if no Haiku run is present in the build context, it falls back to a placeholder (handled by ADR-0011 follow-up).

### Documentation drift

- `README.md` line 42 has unresolved placeholder cells: `<HAIKU_ACCURACY>`, `<HAIKU_NON_ADV>`, `<HAIKU_ADV>`, `<HAIKU_COST>`. The committed Haiku run has real values (62.9% / 65.3% / 41.9% / $0.34) that should fill these.
- `README.md` references ADRs with broken paths: `0004-categorizer-protocol.md` (real file: `0004-eval-harness-architecture.md`), `0005-single-model-v0.md` (real: `0005-single-model-categorizer-v0.md`), and `0008-env-var-list-parsing.md` (no ADR-0008 exists at all).
- `README.md` claims "ADRs: 10" in the badge, but 15 ADRs are accepted (with 0008 skipped). Either correct the count or actually write 0008.
- `docs/ARCHITECTURE.md` describes pgvector retrieval, corrections-driven learning, an LLM-as-judge for reasoning quality, and observability that are not built. Sections are not labeled with implementation status.
- `docs/PRODUCT_BRIEF.md` does not exist; sessions referenced it as a source.

### Eval methodology issues

- Per-category precision/recall is computed by `metrics.per_category_precision_recall` using the full ground truth on every slice. When called on the adversarial slice, only adversarial predictions are in the `predictions` list — that's correct — but the ground truth dict still contains all 302 transactions. Result: support values are inflated and recall numbers for low-support categories are misleading. This is documented here as a known issue, not yet fixed.
- The reliability diagram exists but there's no expected calibration error (ECE) summary number.
- There's no baseline comparison beyond stub-vs-model.

---

## 3. What needs to exist for LedgerLens to be a working bookkeeping app

The minimum end-to-end workflow:

1. **Intake.** Upload a CSV of bank transactions OR POST individual transactions. Normalize the description, preserve the raw, persist.
2. **Categorize.** Run the categorizer (Haiku, optionally with a rule layer) over each transaction. Persist the result with confidence, explanation, latency, cost, and model metadata.
3. **Route.** Apply the confidence thresholds. Mark each as `auto_approved`, `needs_review`, or `uncategorizable`.
4. **Review.** Show a queue of `needs_review` transactions. Reviewer approves, corrects, or marks uncategorizable. Each decision is audited.
5. **Learn.** Corrections feed back into future categorizations (initially as a deterministic merchant-keyed lookup; pgvector retrieval is future work).
6. **Export.** Generate a CSV of the finalized ledger with the resolved category per transaction.
7. **Audit.** Every state change writes an `AuditEvent` row; an endpoint lists them.

Underneath all that:

- A real persistence layer (SQLite for demo, Postgres for any real deployment).
- A configuration model that lets the backend start without a model-provider key (so the health-check works in any environment, regardless of secrets).
- A frontend that actually drives the workflow, not only a marketing page + an eval dashboard.

---

## 4. Priority ranking of missing capabilities

| # | Capability | Why this rank |
|---|---|---|
| 1 | Backend startup hardening (lazy settings, real `/health`, `/ready`) | Everything else assumes the app can start. Currently fragile in deployed environments. |
| 2 | Persistence layer (SQLAlchemy + Alembic + SQLite) | Every product endpoint downstream needs to persist. |
| 3 | Transaction intake API (`POST /transactions`, list, get) | Without this, there's no way to put data into the system through HTTP. |
| 4 | Categorize endpoint (`POST /categorize`) | The product's headline feature, currently only exists as a batch eval. |
| 5 | Confidence routing → status | Specified in ADR-0005 and architectures docs; not implemented. |
| 6 | Review queue endpoints (approve / correct / uncategorizable) | The human-in-the-loop pillar the marketing page advertises. |
| 7 | Audit events | "Auditable by design" is one of the three pillars; needs to be more than a tagline. |
| 8 | Ledger export (CSV) | The actual deliverable a bookkeeper would want. |
| 9 | Frontend workflow pages (`/app`, `/transactions`, `/review`, `/ledger`) | Without this the product isn't usable from a browser. |
| 10 | CSV import endpoint | Real bookkeepers don't paste JSON. |
| 11 | Doc cleanup (placeholders, ADR links, status labels) | Stops the repo from looking unfinished to reviewers. |
| 12 | Correction memory (deterministic merchant lookup) | Closes the feedback loop. |
| 13 | Hybrid rules + model categorizer | Better accuracy on obvious cases at lower cost. |
| 14 | Eval harness fixes (sliced metrics, ECE, baseline rules) | Improves the project's evaluation credibility. |
| 15 | Security baseline (input limits, log redaction, narrow CORS) | Required for credibility on financial data. |
| 16 | Observability (structured logs, request IDs, cost tracking endpoint) | Operational basics. |
| 17 | Demo data + seed flow | Enables a recruiter to click through the workflow. |
| 18 | CI gates (no-placeholder check, eval schema validation) | Stops drift from recurring. |
| 19 | Deployment hardening (env var docs, Dockerfile review post-changes) | Last-mile, after the rest works. |
| 20 | Frontend test framework | Lowest priority — useful but not blocking. |

---

## 5. Risks and technical debt

- **`get_settings()` at import time** couples app start to env-var presence. Tests work around it; production deploys require both `ANTHROPIC_API_KEY` and `DATABASE_URL` to be set even though they're not needed for `/health`. Risk: hard to debug deployment failures.
- **No request-ID propagation or structured logging.** Once multiple endpoints exist, debugging a misbehaving categorization across the categorize/route/store/audit path will be miserable.
- **`evals/datasets/v0/` is duplicated implicitly.** The dataset is the source of truth for the chart of accounts in three businesses; the product code does not currently read it as the COA source. If the product seeds a real COA, the two will drift.
- **No transaction-level idempotency.** A re-import of the same CSV will create duplicate transactions. Either a transaction hash key or an explicit `external_id` is needed.
- **CSV parsing has zero hardening.** The intake endpoint will need size limits, encoding detection, and header normalization.
- **The categorizer cost numbers in `claude_haiku.py` are point-in-time constants.** No mechanism to update them; no warning if usage moves outside the assumed model.
- **Frontend dashboards read JSON at build time.** Any new run requires a redeploy. Documented but not solved.
- **Single-tenant assumption.** Acceptable for v0, but the data model should not embed it permanently.

---

## 6. Definition of done for this build sprint

The sprint deliverable is **a backend that drives the bookkeeping workflow end-to-end via HTTP**, with honest documentation. Specifically:

1. The Python app starts and serves `/health` without any provider env vars set.
2. A `/ready` endpoint reports database connectivity separately.
3. A SQLite-backed data model exists with: `AccountCategory`, `Transaction`, `CategorizationResult`, `ReviewDecision`, `AuditEvent`.
4. HTTP endpoints exist for:
   - Transaction creation, listing, retrieval (single + batch + CSV import)
   - Single-transaction and batch categorization, persisting results with confidence routing
   - Review queue listing, approval, correction, and uncategorizable marking
   - Ledger listing and CSV export
   - Audit event listing
5. Tests cover each endpoint's happy path and at least one failure mode.
6. The README accurately states what is shipped versus what is in progress versus what is planned.
7. Frontend workflow pages and the corrections loop are explicitly tagged as "next sprint" — this gap-analysis doc names them, but the gap is not closed in this sprint.

What is **explicitly not in scope** this sprint:

- Frontend workflow pages (Phases 8–9 in the staff-engineer brief)
- Eval harness improvements (Phase 10)
- Hybrid rules + model categorizer (Phase 11)
- Correction-memory retrieval (Phase 12)
- Full security baseline and observability layer (Phases 13–14)
- Demo-data polish (Phase 15)
- CI quality gates beyond what already exists (Phase 17)
- Deployment hardening passes (Phase 18)
- Final self-review document (Phase 20)

Each of those gets its own session.

---

## 7. Sequencing for follow-up sessions

A reasonable order after this PR:

1. **Frontend workflow pages.** Once the backend has real endpoints, the frontend's `/transactions`, `/transactions/import`, `/transactions/[id]`, `/review`, `/ledger`, `/app` (dashboard) pages can be built. Adds a typed API client and shared error rendering. (Phases 8–9.)
2. **Correction memory.** Deterministic merchant-keyed lookup, with an endpoint that lists learned corrections. (Phase 12.)
3. **Hybrid categorizer.** Rule layer first, model fallback. New `HybridCategorizer` behind the same Protocol. (Phase 11.)
4. **Eval harness fixes + new metrics.** Sliced per-category metrics, ECE, routing metrics, baseline rule comparison. (Phase 10.)
5. **Security and observability.** Log redaction utility, input limits, structured logs with request IDs, operational summary endpoint. (Phases 13–14.)
6. **Demo data + seed flow.** Reset endpoint, demo-mode banner on the frontend, scripted walkthrough. (Phase 15.)
7. **CI and deployment hardening.** No-placeholder check, schema validation for eval artifacts, env-var docs by mode. (Phases 17–18.)
8. **Final self-review.** `docs/FINAL_ENGINEERING_REVIEW.md`, README pointer. (Phase 20.)
