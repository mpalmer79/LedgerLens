# LedgerLens — Evals

Eval harness for LedgerLens categorization. See [../docs/ARCHITECTURE.md](../docs/ARCHITECTURE.md) §7 for the methodology and gating rules. Test sets live in `datasets/`, the harness lives in `harness/`, run outputs land in `runs/` (gitignored).
